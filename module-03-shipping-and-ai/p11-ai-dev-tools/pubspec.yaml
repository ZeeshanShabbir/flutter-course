name: p11_ai_dev_tools
description: >
  P11 — AI Dev Tools.
  ChatGPT, GitHub Copilot, Cursor IDE — using AI in your
  daily Flutter development workflow. Prompt engineering
  for code generation, debugging, and test writing.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
